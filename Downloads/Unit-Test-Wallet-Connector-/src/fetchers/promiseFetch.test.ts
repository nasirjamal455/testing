import { fetchWithStore } from './promiseFetch';

// TODO: Write more tests for repo!
describe('fetchWithStore', () => {
  test('exists', () => {
    expect(Boolean(fetchWithStore)).toBeTruthy();
  });
});
